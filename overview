CREATE USER 'root1'@'localhost' IDENTIFIED WITH mysql_native_password BY 'password';
GRANT ALL PRIVILEGES ON *.* TO 'root1'@'localhost' WITH GRANT OPTION;

create database seven;

accessing mysql within laravel >mysql -u root -p
